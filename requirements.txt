# Omega Runtime Studio — runtime deps (Python 3.11+)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
typer>=0.12
PySide6>=6.6
huggingface_hub>=0.23
tqdm>=4.66
psutil>=5.9
# Pinned to 0.3.22 because that's the version we ship the Vulkan-built
# DLLs for (ggml-vulkan.dll + matching llama.dll). Bumping the version
# without rebuilding/replacing those DLLs reverts the bundle to a
# CPU-only wheel. Variant builds (CUDA / DML / CPU-only) override via
# OMEGA_LLAMA_CPP_INDEX in scripts/build_variants.ps1.
llama-cpp-python==0.3.22
onnxruntime>=1.18,<2.0
tokenizers>=0.20
