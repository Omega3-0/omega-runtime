Place harvested accelerator binaries under vendor\accelerators\bin after running:

  .\scripts\sync_vendor_accelerators.ps1 -VendorDest <this repo root>

Legacy layout vendor\lemonade\bin is still recognized at runtime if you have not migrated yet.
